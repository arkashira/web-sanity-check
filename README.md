# Web Sanity Check

A tool to help prioritize stabilization of flaky tests.

## Usage

1. Create an instance of `WebSanityCheck`.
2. Add test results using `add_test_result`.
3. Get flaky tests using `get_flaky_tests`.
4. Mark a test as investigated using `mark_test_as_investigated`.
