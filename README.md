# Web Sanity Check

A GitHub App that triggers a test run by commenting `/wsc test` on a PR.

## Usage

1. Install the GitHub App.
2. Comment `/wsc test` on a PR.
3. The app will trigger a test run and reply with a status comment.
