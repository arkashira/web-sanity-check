# Web Sanity Check

A GitHub Action that triggers a test run and fails the workflow on test or security failures.

## Usage

1. Create a new GitHub Actions workflow file (e.g., `.github/workflows/web-sanity-check.yml`).
2. Add the following YAML code to the file:
